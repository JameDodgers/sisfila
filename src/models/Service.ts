export type Service = {
  id: string;
  name: string;
  subscriptionToken: string;
  guestEnroll: boolean;
  opensAt: string;
  closesAt: string;
  createdAt: string;
  updatedAt: string;
  organizationId: string;
};
